import {atom} from 'recoil';

export const isActiveState = atom({
    key: "isActive",
    default: ""
})

export const navMapState = atom({
    key: "navMapActive",
    default: ""
})

export const navState = atom({
    key: "navActive",
    default: ""
})

export const schWordState = atom({
    key: "schWord",
    default: ""
})

